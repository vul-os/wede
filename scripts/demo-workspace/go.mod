// Standalone sample project used only for documentation screenshots.
// It is a separate module so it is excluded from wede's `go build ./...`.
module taskboard

go 1.23

require github.com/mattn/go-sqlite3 v1.14.22
